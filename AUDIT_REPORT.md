# 📊 ПОЛНЫЙ АУДИТ: Wiki

> 🔄 Трёхпроходная верификация | 6 экспертных критериев | 2026-02-26

## Сводка

| Критерий | Раунд 1 | Раунд 2 | Финал | Уровень |
|----------|---------|---------|-------|---------|
| 🎨 Frontend | 4/10 | 4/10 | **4/10** | 🟠 Низкий |
| ⚙️ Backend | 7/10 | 7/10 | **7/10** | 🟢 Хороший |
| 🔒 Security | 5/10 | 4/10 | **4/10** | 🟠 Низкий |
| 🧪 QA | 4/10 | 4/10 | **4/10** | 🟠 Низкий |
| 🏗️ DevOps | 6/10 | 5/10 | **5/10** | 🟡 Средний |
| 🎯 UI/UX | 6/10 | 7/10 | **7/10** | 🟢 Хороший |
| **Итого** | 32/60 | 31/60 | **31/60** | **🟡 Средний** |

## Детальный разбор по каждому критерию

### 🎨 Frontend — 4/10
**Проблемы:**
- **Отсутствие тестов:** В проекте `frontend/` нет настроенного Jest/Vitest, покрытие кода 0%. (Файл `frontend/package.json`).
- **Отсутствие server-state менеджмента:** Данные загружаются через сырые `fetch` в `useEffect` без кеширования, инвалидации и оптимистичных обновлений (напр. компонент `AdminPanel.tsx` и страницы).

**Рекомендации:**
- Внедрить `vitest` и `@testing-library/react`.
- Добавить React Query (`@tanstack/react-query`) или `RTK Query` для API-запросов.

### ⚙️ Backend — 7/10
**Проблемы:**
- **Проблема N+1 / Загрузка в память:** В `pages.py` (`/tree`) исполняется полное сканирование (`select(Page)`) списка страниц, что приведет к исчерпанию памяти при увеличении числа записей в таблице. Загрузка дерева не ленивая.

**Рекомендации:**
- Реализовать lazy-loading дерева (подгружать детей по клику) или ограничить уровень вложенности (`maxdepth`) в SQL, используя возможности `Ltree`.

### 🔒 Security — 4/10
**Проблемы:**
- **Хардкод секретов:** Файл `app/core/config.py` содержит хардкод `SECRET_KEY`, а в `docker-compose.yml` указаны production-подобные пароли `wiki_password` и `minioadmin123`.

**Рекомендации:**
- Убрать plaintext пароли из `config.py` и `docker-compose.yml`, использовать `.env`. Настроить менеджер секретов или хотя бы валидацию их обязательного наличия через pydantic без fallback-значений для production.

### 🧪 QA — 4/10
**Проблемы:**
- **Фронтенд не тестируется:** Отсутствуют юнит-тесты и E2E тесты (Cypress, Playwright не установлены).
- **Сбор покрытия:** В `.github/workflows/ci.yml` пайплайн для бекенда запускает `pytest`, но не проверяет покрытие бизнес-логики (`--cov`).

**Рекомендации:**
- Добавить тесты на React-компоненты.
- Настроить шаг Coverage Threshold (например, 70%) в Github Actions для бекенда.

### 🏗️ DevOps — 5/10
**Проблемы:**
- **Ограниченный пайплайн:** В `.github/workflows/ci.yml` отсутствуют проверки безопасности образов (Trivy), нет стадии CD (автоматического деплоя на staging). 

**Рекомендации:**
- Разделить сборку образов, добавить security сканирование (Snyk/Trivy) в CI пайплайн. Реализовать стратегию автоматического или ручного деплоя. 

### 🎯 UI/UX — 7/10
**Проблемы:**
- **Доступность (A11y):** В проекте используется Ant Design, который частично покрывает a11y, но нет собственных проверок и тестов ARIA-лейблов для кастомных компонентов (например, в SidebarTree или MediaUploader).

**Рекомендации:**
- Провести аудит с помощью `axe-core` или Lighthouse A11y. Убедиться, что навигация с клавиатуры полностью поддерживается для кастомного дерева (`rc-tree` / `antd Tree`).

## 🔄 Изменения между раундами
- **Security:** Оценка снижена с 5 до 4 из-за обнаружения хардкода паролей в `docker-compose.yml` наряду с `config.py`.
- **DevOps:** Оценка снижена с 6 до 5 из-за отсутствия security steps (Trivy) и CD процессов в Github Actions.
- **UI/UX:** Оценка повышена с 6 до 7 после проверки качественной интеграции Dark mode в файле `theme.tsx` и дизайн-токенах.

## 🏆 Топ-5 приоритетных действий
1. **Security:** Удалить хардкод секретов из `config.py` и `docker-compose.yml` (использовать `.env`).
2. **Frontend Architecture:** Внедрить TanStack Query для правильной работы с серверным стейтом.
3. **QA:** Настроить инфраструктуру тестирования фронтенда (Vitest + React Testing Library) и покрытие в CI.
4. **Backend:** Переделать роут `/tree` на ленивую транзакционную загрузку, чтобы избежать OOM-сбоев при большой базе.
5. **DevOps:** Добавить шаги Security Scanning (Trivy) и настроить полноценный CI/CD pipeline с деплоем.
