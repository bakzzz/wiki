---
description: Ревизор — жёсткая мета-проверка качества любого ревью или аудита. Проверяет полноту, глубину, честность оценок и наличие RTM.
---

# Ревизор

Запускает мета-проверку ранее выполненного ревью/аудита. Выявляет пропуски, поверхностный анализ, завышенные баллы.

## Шаг 1: Загрузить скилл

1. Прочитать `~/.agent/skills/review-revisor/SKILL.md` — полностью.
2. Прочитать критерии из `~/.agent/skills/review-revisor/references/revisor-criteria.md`.

## Шаг 2: Определить входные данные

2. Определить что ревизировать:
   - Если есть отчёт аудита (например `AUDIT_REPORT.md`) → использовать его
   - Если ревью было в чате → собрать все оценки и замечания
   - Если пользователь указал конкретный файл → использовать его

   Загрузить соответствующие **чеклисты** из `~/.agent/skills/fullstack-review/references/`:
   - `frontend.md`, `backend.md`, `security.md`, `qa.md`, `devops.md`, `ux.md`

// turbo
3. Подсчитать общее количество пунктов в каждом чеклисте:
```bash
for f in ~/.agent/skills/fullstack-review/references/*.md; do echo "$(basename $f): $(grep -c '^\s*-' $f) items"; done
```

## Шаг 3: Проверка полноты (Coverage Audit)

4. Для каждого из 6 критериев проверить:
   - [ ] **Каждый пункт** чеклиста адресован в отчёте (не пропущен)
   - [ ] Пропущенные пункты — перечислить
   - [ ] Процент покрытия: `адресовано / всего × 100%`

   Критерии прохождения загрузить из `references/revisor-criteria.md`.

## Шаг 4: Проверка Logic Check (RTM Audit)

5. Если задача выполнялась с `logic-check` — проверить:
   - RTM существует? (нет RTM при ≥ 3 шагов → штраф -5 баллов)
   - RTM полная? (пропущенные требования → перечислить)
   - RTM с доказательствами? (✅ Done → есть файл:строка?)
   - IN-FLIGHT выполнялся? (3 вопроса после каждого файла?)
   - POST-CHECK есть? (чеклист + CoVe)

## Шаг 5: Строковый контроль кода (Line-by-Line Audit)

6. Для каждого замечания в отчёте проверить:
   - [ ] Указан **конкретный файл** (не «в коде»)
   - [ ] Указана **конкретная строка** или функция
   - [ ] Описание **конкретное** (не «можно улучшить»)
   - [ ] Предложено **конкретное решение**

   Замечания без файла:строки → пометить как **поверхностные**.

## Шаг 6: Честность оценок (Score Audit)

7. Для каждой оценки (0-10) проверить:
   - [ ] Оценка **обоснована** конкретными фактами
   - [ ] Оценка ≥ 8 подтверждена **strong evidence**
   - [ ] Оценка ≤ 3 имеет **примеры critical issues**
   - [ ] Нет **lazy scores** (все оценки одинаковые)
   - [ ] Оценки **консистентны** (много проблем ≠ высокий балл)

   Если оценка завышена → предложить скорректированную с обоснованием.

## Шаг 7: Финальный отчёт ревизора

8. Сформировать отчёт:

   ```
   🔍 REVISOR REPORT
   ═══════════════════

   📊 Coverage:
     Frontend: [X/Y] пунктов покрыто ([Z]%)
     Backend: [X/Y] ([Z]%)
     ...

   🎯 Scores Audit:
     Оригинал → Скорректированный (обоснование)
     Frontend: 7 → 7 ✅
     Security: 8 → 5 ⚠️ (3 критичных пропуска)

   📋 Logic Check:
     RTM: [есть/нет] [полная/неполная]
     IN-FLIGHT: [выполнялся/нет]

   ⚠️ Поверхностные замечания: [N]
   ❌ Пропущенные пункты: [N]

   📝 Рекомендации:
     1. [что доделать]
     2. [что перепроверить]
   ```

## Шаг 8: Вердикт

9. Выдать вердикт:
   - **✅ PASS** — ревью качественное, можно использовать
   - **⚠️ PARTIAL** — есть пропуски, нужно дополнить
   - **❌ FAIL** — ревью поверхностное, требуется переделка

   Показать отчёт пользователю.

## Ограничения

- ❌ НЕ пропускать пункты чеклиста — проверить ВСЕ
- ❌ НЕ принимать оценки без обоснования
- ❌ НЕ считать замечания без файла:строки конкретными
- ❌ НЕ завышать вердикт — быть жёстким, но справедливым
