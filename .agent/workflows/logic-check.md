---
description: Проверить логику работы — PRE/IN-FLIGHT/POST проверка с RTM, обнаружение конфликтов требований, валидация связности.
---

# Проверить логику

Запускает полный цикл logic-check для текущей задачи: PRE-CHECK → IN-FLIGHT → POST-CHECK.

## Шаг 1: Загрузить скилл

1. Прочитать `~/.agent/skills/logic-check/SKILL.md` — полностью, включая все 3 фазы и шаблоны.

## Шаг 2: Определить режим

2. Определить какой режим запускать:

   | Ситуация | Режим |
   |----------|-------|
   | Задача ещё не начата | **PRE-CHECK** → создать RTM |
   | Задача в процессе | **IN-FLIGHT** → 3 вопроса |
   | Задача завершена | **POST-CHECK** → финальная сверка |
   | Пользователь говорит «сверь с ТЗ» | **POST-CHECK** немедленно |
   | Полная проверка | **PRE + IN-FLIGHT + POST** |

## Шаг 3: PRE-CHECK (до выполнения)

3. Извлечь ВСЕ требования из запроса пользователя или ТЗ:

   ```
   | # | Требование | Тип | Файл | Строка | Статус | Версия |
   |---|-----------|-----|------|--------|--------|--------|
   | 1 | [что сделать] | Явное | — | — | 🔲 TODO | v1 |
   ```

   - Типы: **Явное**, **Неявное**, **Ограничение**
   - Проверить конфликты для КАЖДОЙ пары (i, j)
   - При конфликте ❌ или ⚠️ → ОСТАНОВИТЬСЯ, спросить пользователя
   - Сгенерировать чеклист верификации

## Шаг 4: IN-FLIGHT CHECK (во время выполнения)

4. После **каждого** изменённого файла ответить на 3 вопроса:

   ```
   1. СВЯЗЬ: Это изменение связано с конкретным требованием RTM? Каким?
   2. БЕЗОПАСНОСТЬ: Это изменение НЕ ЛОМАЕТ существующую функциональность?
   3. ПОЛНОТА: Нет ли пропущенных требований, зависящих от этого шага?
   ```

   - Обновить RTM (статус, файл, строка)
   - При НОВОМ требовании от пользователя → Conflict Check → Impact Check

## Шаг 5: POST-CHECK (после выполнения)

5. Финальная проверка:

   **5.1 Пройти чеклист** — для каждого пункта ответ Yes/No с доказательством:
   ```
   [✅] Требование #1 → Да, file.py:L42
   [❌] Требование #3 → Нет, ПРОПУЩЕНО
   ```

   **5.2 Сверить RTM** — для каждого требования:
   - ✅ Done → файл:строка
   - ❌ Miss → почему не выполнено
   - ⛔ Отменено → кем и когда

   **5.3 Контрольные вопросы (CoVe)** — 3 вопроса:
   ```
   "Если пользователь [действие], он увидит [ожидаемый результат]?"
   ```

   **5.4 Финальный отчёт:**
   ```
   📋 LOGIC CHECK REPORT
   ═════════════════════
   RTM: [N/M] требований выполнено ([X]% покрытие)
   Чеклист: [N/M] пунктов ✅
   Конфликты: [N] обнаружено, [N] разрешено
   Контрольные вопросы: [N/3] ✅
   ```

## Шаг 6: Обработка изменений ТЗ

6. Если пользователь меняет требования во время работы:

   - ОСТАНОВИТЬСЯ
   - Добавить требование с 🆕 и новой версией
   - Conflict Check всех существующих
   - Impact Check для уже сделанных пунктов (🔁 Requires Rework)
   - Обновить чеклист

## Ограничения

- ❌ НЕ разрешать конфликты самостоятельно — только обнаруживать и спрашивать
- ❌ НЕ удалять требования из RTM — только помечать ⛔
- ❌ НЕ пропускать IN-FLIGHT даже «для быстроты»
- ❌ НЕ считать неявные требования подтверждёнными — спросить пользователя
