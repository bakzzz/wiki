---
description: Ознакомление с новым проектом, чатом, контекстом, правилами и скилами. Запускать в начале работы с любым новым проектом или при входе в новый контекст.
---

# Ознакомление с проектом

// turbo-all

## Шаг 1: Контекст агента

1. Определить корень проекта и прочитать правила агента:
```bash
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd) && cat "$PROJECT_ROOT/.agent/rules.md" 2>/dev/null || echo "Нет .agent/rules.md"
```

2. Прочитать реестр проекта:
```bash
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd) && cat "$PROJECT_ROOT/registry.md" 2>/dev/null || echo "Нет registry.md"
```

3. Посмотреть доступные воркфлоу:
```bash
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd) && ls -la "$PROJECT_ROOT/.agent/workflows/" 2>/dev/null || echo "Нет воркфлоу"
```

## Шаг 2: Доступные скилы

4. Просканировать все скилы (глобальные + проектные) и прочитать их описания:
```bash
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd) && grep -r "^description:" ~/.agent/skills/*/SKILL.md "$PROJECT_ROOT/.agent/skills/*/SKILL.md" 2>/dev/null | sort -u
```

5. Запомнить какие скилы доступны и когда их применять — описания в frontmatter содержат триггеры.

## Шаг 3: Структура проекта

6. Определить корневую директорию проекта и просканировать:
```bash
find . -maxdepth 3 -not -path './.git/*' -not -path './node_modules/*' -not -path './.venv/*' -not -path './vendor/*' -not -path './dist/*' -not -path './build/*' | head -120
```

7. Определить tech stack:
```bash
ls -la package.json requirements.txt go.mod Cargo.toml composer.json Gemfile pom.xml build.gradle *.csproj 2>/dev/null
```

8. Прочитать README проекта (если есть):
```bash
cat README.md 2>/dev/null || cat readme.md 2>/dev/null || echo "README не найден"
```

## Шаг 4: Конфигурация проекта

9. Проверить наличие проект-специфичных правил и конфигов:
```bash
cat .agent/rules.md 2>/dev/null || echo "Нет .agent/rules.md"
ls .agent/workflows/ 2>/dev/null || echo "Нет проект-специфичных воркфлоу"
cat .env.example 2>/dev/null || echo "Нет .env.example"
```

10. Проверить CI/CD и Docker:
```bash
ls -la .github/workflows/ .gitlab-ci.yml Dockerfile docker-compose.yml docker-compose.yaml Makefile 2>/dev/null
```

## Шаг 5: Ключевые файлы

11. Найти entry points и ключевые модули:
```bash
# Точки входа
ls -la src/index.* src/main.* src/app.* app.* main.* index.* manage.py server.* 2>/dev/null
# Конфиги
ls -la tsconfig.json eslint* prettier* .editorconfig jest.config.* vitest.config.* pytest.ini pyproject.toml 2>/dev/null
```

## Шаг 6: История и контекст

12. Посмотреть последние изменения:
```bash
git log --oneline -15 2>/dev/null || echo "Нет git истории"
```

13. Посмотреть открытые ветки:
```bash
git branch -a 2>/dev/null | head -20
```

## Шаг 7: Архитектура инфраструктуры (Dev vs Prod)

14. Проверить наличие Prod-конфигураций и скриптов в глобальной инфраструктуре:
```bash
ls -la ~/dev/antigravity/deploy-to-prod.sh ~/dev/antigravity/sync-from-prod.sh 2>/dev/null
```
*Правило: Текущая локальная машина — это **Dev**. Боевой сервер (dafanasev) — это **Prod**. Деплой кода и стягивание базы данных выполняются строго через скрипты в папке `~/dev/antigravity/`.*

## Шаг 8: Сводка

15. Сформировать краткую сводку для пользователя:

```
📋 СВОДКА ПРОЕКТА

🏗 Stack: [технологии]
📁 Структура: [основные директории]
🔧 Entry point: [точка входа]
📦 Зависимости: [package manager и кол-во]
🧪 Тесты: [есть/нет, фреймворк]
🚀 Инфраструктура: Учитывается разделение Dev (local) / Prod (dafanasev). Скрипты деплоя доступны.
🐳 Docker: [есть/нет локальный compose, где лежит prod compose]
📝 Docs: [README/документация]
🎯 Доступные скилы: [список применимых к этому проекту]
⚡ Воркфлоу: [список доступных команд]
```
