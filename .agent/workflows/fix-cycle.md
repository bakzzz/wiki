---
description: Циклическое исправление проекта на основании аудита. Запускает fixer-скилы по каждому критерию, перепроверяет ревизором, зацикливается пока все оценки не достигнут целевого балла (по умолчанию 10/10).
---

# Цикл исправления

Автоматическое исправление проблем, найденных при аудите, с ревизором на каждой итерации.

## Настройки

- **Целевой балл**: 10/10 по каждому критерию (переопределяется пользователем)
- **Макс. итераций**: 5 (защита от бесконечного цикла)
- **Ревизор**: включён на каждой итерации

## Шаг 0: Входные данные

1. Убедиться что есть результат аудита (отчёт fullstack-review или полного аудита). Если нет — сначала запустить `/полный-аудит`.

2. Из отчёта извлечь текущие оценки и список проблем по каждому критерию. Определить какие критерии ниже целевого балла.

3. Спросить пользователя целевой балл (если не указан, использовать 10/10).

## Шаг 1: Приоритизация

4. Определить порядок исправления — от наиболее критичных:

| Приоритет | Критерий | Причина |
|-----------|----------|---------|
| 1 | 🔒 Security | Уязвимости = максимальный риск |
| 2 | ⚙️ Backend | Фундамент для всего |
| 3 | 🧪 QA | Тесты защищают от регрессий |
| 4 | 🏗️ DevOps | CI/CD обеспечивает стабильность |
| 5 | 🎨 Frontend | UI-проблемы после стабилизации логики |
| 6 | 🎯 UI/UX | Полировка в последнюю очередь |

Пропускать критерии которые уже ≥ целевого балла.

## Шаг 2: Цикл исправления

Для каждой итерации (от 1 до 5):

### 2.1. Исправление

5. Для каждого критерия ниже целевого балла — загрузить и применить соответствующий fixer-skill:

| Критерий | Skill | Путь |
|----------|-------|------|
| 🎨 Frontend | `fix-frontend` | `~/.agent/skills/fix-frontend/SKILL.md` |
| ⚙️ Backend | `fix-backend` | `~/.agent/skills/fix-backend/SKILL.md` |
| 🔒 Security | `fix-security` | `~/.agent/skills/fix-security/SKILL.md` |
| 🧪 QA | `fix-qa` | `~/.agent/skills/fix-qa/SKILL.md` |
| 🏗️ DevOps | `fix-devops` | `~/.agent/skills/fix-devops/SKILL.md` |
| 🎯 UI/UX | `fix-ux` | `~/.agent/skills/fix-ux/SKILL.md` |

Для каждого fix:
- Загрузить SKILL.md фиксера
- Прочитать конкретные проблемы из аудита для этого критерия
- Применить исправления по стратегиям из skill
- **Соблюдать ограничения (❌ НЕ делать)** из skill — не трогать чужой домен
- Записать что было изменено: файл, строка, что было → что стало

### 2.2. Ревизор изменений

6. После всех исправлений итерации запустить ревизора:

   - Загрузить `~/.agent/skills/review-revisor/SKILL.md`
   - Проверить что fix-и не сломали другой код:
     - Запустить lint/type-check/build (если настроены)
     - Запустить существующие тесты
     - Проверить что fixer не вышел за границы своего домена
   - Проверить качество каждого fix-а:
     - Минимальное изменение? Не переписали ли весь файл?
     - Fix решает конкретную проблему из аудита?
     - Нет побочных эффектов?

   Если ревизор находит проблемы с fix-ами → откатить проблемные изменения и переделать.

### 2.3. Повторный аудит

7. Запустить повторный fullstack-review по исправленному проекту:

   - Загрузить `~/.agent/skills/fullstack-review/SKILL.md`
   - Проверить все 6 критериев заново
   - Сравнить новые оценки с предыдущими

### 2.4. Проверка прогресса

8. Оценить результат итерации:

```
📊 Итерация N — Прогресс:

| Критерий | Было | Стало | Цель | Статус |
|----------|------|-------|------|--------|
| 🎨 Frontend | X | Y | 10 | [✅ достигнуто / 🔄 продолжить] |
| ... | ... | ... | ... | ... |
```

   - Если **все критерии ≥ целевого балла** → перейти к Шагу 3 (финал)
   - Если **нет прогресса** по какому-то критерию 2 итерации подряд → спросить пользователя
   - Если **итерация 5** и не все цели достигнуты → перейти к Шагу 3 с отчётом

   Иначе → вернуться к 2.1 со списком оставшихся проблем.

## Шаг 3: Финальный отчёт

9. Сгенерировать итоговый отчёт:

```
# 🔧 ОТЧЁТ ЦИКЛА ИСПРАВЛЕНИЙ

> Итераций: N | Целевой балл: X/10 | [Дата]

## Прогресс по итерациям

| Критерий | Исходный | Итер.1 | Итер.2 | ... | Финал | Цель |
|----------|----------|--------|--------|-----|-------|------|
| 🎨 Frontend | X | Y | Z | ... | **W** | 10 |
| ⚙️ Backend | X | Y | Z | ... | **W** | 10 |
| 🔒 Security | X | Y | Z | ... | **W** | 10 |
| 🧪 QA | X | Y | Z | ... | **W** | 10 |
| 🏗️ DevOps | X | Y | Z | ... | **W** | 10 |
| 🎯 UI/UX | X | Y | Z | ... | **W** | 10 |

## Все изменения
[Список файлов, что было изменено и почему]

## Вердикт ревизора
[Итоговый вердикт — всё ли безопасно]

## Нерешённые проблемы (если есть)
[Что не удалось исправить и почему]
```

10. Предложить сохранить отчёт в `FIX_REPORT.md` в корне проекта.
