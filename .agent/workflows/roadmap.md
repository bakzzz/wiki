---
description: Сформировать дорожную карту проекта — декомпозиция задачи на фазы и шаги с обязательными артефактами. Использует incremental-progress (MAL цикл).
---

# Сформировать дорожную карту

Создаёт структурированный план работы с Progress Map, декомпозицией на фазы и шаги, обязательными артефактами и точками проверки.

## Шаг 1: Загрузить скилл

1. Прочитать `~/.agent/skills/incremental-progress/SKILL.md` — полностью, включая все фазы MAL.

## Шаг 2: Извлечение цели

2. Определить из запроса пользователя или из `docs/technical-requirements.md`:
   - 🎯 **Цель**: одно предложение — что в итоге должно получиться
   - 📦 **Артефакт-результат**: что конкретно будет создано/изменено
   - ⏱️ **Оценка**: сколько шагов / файлов

   Если цель неясна — спросить пользователя. НЕ додумывать.

## Шаг 3: Декомпозиция

3. Разбить задачу на уровни:

   ```
   Уровень X — Фаза (крупный блок работы)
     └─ Уровень X.Y — Подзадача (конкретное действие)
          └─ Уровень X.Y.Z — Мелкий шаг (одна правка)
   ```

   **Правила:**
   - Один шаг = одно конкретное действие (создать файл, изменить функцию, добавить тест)
   - Каждый шаг имеет проверяемый результат (тест проходит, файл создан, build зелёный)
   - Максимум 10-15 минут на шаг
   - Зависимости явные

## Шаг 4: Создать Progress Map

4. Используя шаблон из `~/.agent/skills/incremental-progress/references/progress-map-template.md`, создать Progress Map:

   ```markdown
   ## Progress Map — [задача]

   🎯 Цель: [одно предложение]
   📦 Результат: [что получится]
   ⏱️ Шагов: [N]

   ### Фаза 1: [название]
   - [ ] 1.1 [действие] → [артефакт] → [проверка]
   - [ ] 1.2 [действие] → [артефакт] → [проверка]
   📌 Checkpoint: summary после фазы 1

   ### Фаза 2: [название]
   - [ ] 2.1 [действие] → [артефакт] → [проверка]
   📌 Checkpoint: summary после фазы 2
   ```

## Шаг 5: RTM (если ≥ 3 шагов)

5. Если задача ≥ 3 шагов, параллельно создать **RTM** (Requirement Traceability Matrix):
   - Прочитать `~/.agent/skills/logic-check/SKILL.md` → Фаза 1 PRE-CHECK
   - Извлечь все требования (явные + неявные)
   - Проверить конфликты между требованиями
   - Добавить RTM-таблицу в Progress Map

## Шаг 6: Показать пользователю

6. Показать готовую дорожную карту пользователю для утверждения:
   - Progress Map с фазами и шагами
   - RTM (если создана)
   - Предлагаемый порядок реализации
   - Спросить: "Всё ли учтено? Правильный ли порядок? Нужно ли что-то убрать или добавить?"

   **ПРАВИЛО:** НЕ начинать работу без подтверждения плана.

## Шаг 7: Начать работу (после подтверждения)

7. После утверждения — начать MAL-цикл:
   - **DO**: выполнить шаг [1.1]
   - **CHECK**: проверить (артефакт есть? проверка пройдена?)
   - **SAVE**: обновить Progress Map + task.md
   - **REPORT**: статус-футер с прогрессом

   Повторять для каждого шага.

## Шаг 8: Checkpoint каждой фазы

8. После завершения каждой фазы:
   - Написать Progressive Summary
   - Git commit
   - Обновить walkthrough.md
   - Перейти к следующей фазе

## Ограничения

- ❌ НЕ начинать без утверждения плана пользователем
- ❌ НЕ пропускать шаги «для быстроты»
- ❌ НЕ объединять несколько шагов в один
- ❌ НЕ переходить к следующему шагу без CHECK текущего
