# Conflict Detection Rules — Правила обнаружения конфликтов

## Алгоритм проверки

При добавлении требования #N в RTM:

```
for each existing requirement #X (X = 1..N-1):
    answer: "Может ли #N противоречить #X?"
    
    if ПРЯМОЙ КОНФЛИКТ:
        → БЛОК. Показать оба. Спросить пользователя.
    
    if КОСВЕННЫЙ КОНФЛИКТ:
        → ПРЕДУПРЕЖДЕНИЕ. Показать последствия. Предложить варианты.
    
    if ЗАМЕНА:
        → Пометить старое ⛔. Продолжить с новым.
    
    if НЕТ КОНФЛИКТА:
        → Продолжить.
```

---

## Матрица конфликтов по типам действий

### ❌ Прямые конфликты (БЛОК — спрашивать пользователя)

| Действие A | Действие B | Пример |
|-----------|-----------|--------|
| Добавить сущность X | Удалить сущность X | «Добавь поле» vs «Удали поле» |
| X обязательное | X необязательное | `required=True` vs `required=False` |
| Не трогать файл F | Изменить файл F | «Не трогай models.py» vs «Добавь поле в модель» |
| Использовать библиотеку A | Не использовать библиотеку A | «Используй lodash» vs «Без зависимостей» |
| Удалить функцию | Расширить функцию | «Удали validate()» vs «Добавь проверку в validate()» |
| Публичный доступ | Приватный доступ | «Открой API» vs «Закрой API» |

### ⚠️ Косвенные конфликты (ПРЕДУПРЕЖДЕНИЕ — объяснить последствия)

| Действие A | Действие B | Почему конфликт |
|-----------|-----------|----------------|
| UNIQUE constraint | OPTIONAL поле | NULL значения: зависит от СУБД |
| CI/CD pipeline | «Не добавлять файлы» | CI нужны файлы в .github/ |
| Хранить секреты локально | Синхронизировать всё на сервер | Секреты уйдут на сервер |
| Async операция | Синхронный return | Async изменяет control flow |
| Кэширование | Реальное время | Кэш = задержка данных |
| Soft delete | Cascade delete | Удалённые записи ломают каскад |

### 🔄 Замена (пометить старое ⛔, применить новое)

| Старое | Новое | Действие |
|--------|-------|----------|
| Использовать int ID | Использовать UUID | Заменить, проверить миграции |
| SSH авторизация | HTTPS авторизация | Заменить, обновить remote |
| PostgreSQL | SQLite | Заменить, проверить совместимость |
| REST API | GraphQL | Полная переделка API слоя |
| Monorepo | Per-project repos | Реструктуризация |

---

## Impact Check — проверка влияния нового требования

При добавлении 🆕 требования проверить:

```
1. ОБРАТНОЕ ВЛИЯНИЕ — затрагивает ли УЖЕ СДЕЛАННЫЕ пункты?
   → Если да: пометить как 🔁 Требует переделки

2. КАСКАДНОЕ ВЛИЯНИЕ — создаёт ли НОВЫЕ неявные требования?
   → Если да: добавить в RTM как Неявное

3. SCOPE ВЛИЯНИЕ — выходит ли за рамки задачи?
   → Если да: предупредить пользователя
```

### Примеры Impact Check

**Пример 1: Обратное влияние**
```
RTM v1: #1 "email optional" → ✅ Done (user.py:L42, blank=True)
RTM v2: #3 🆕 "email required"

Impact: #3 конфликтует с уже сделанным #1
→ Пометить #1 как 🔁
→ Сообщить: "Нужно изменить user.py:L42 — blank=True → blank=False"
```

**Пример 2: Каскадное влияние**
```
RTM v2: #3 🆕 "использовать UUID вместо int ID"

Каскад:
  → #4 (неявное): обновить миграции
  → #5 (неявное): обновить связанные FK
  → #6 (неявное): обновить сериализаторы
  → #7 (неявное): обновить тесты
```

---

## Формат сообщения пользователю при конфликте

```
⚠️ Обнаружен конфликт в требованиях:

Требование #1 (v1): "[текст]"
  Статус: ✅ Done (файл:строка)

Требование #3 (v2): "[текст]"  🆕
  Тип конфликта: ❌ Прямой

Последствия:
  A) Оставить #1 → [что произойдёт]
  B) Оставить #3 → [что произойдёт, что нужно переделать]
  C) Совместить → [возможно ли, как]

Какой вариант?
```

---

## Приоритеты при множественных конфликтах

Если несколько конфликтов одновременно — разрешать в порядке:

1. 🔴 **Безопасность** — конфликты, затрагивающие секреты, авторизацию, доступ
2. 🟠 **Данные** — конфликты, затрагивающие схему БД, миграции, целостность
3. 🟡 **Логика** — конфликты в бизнес-логике, API контрактах
4. 🟢 **Стиль** — конфликты в именовании, форматировании, структуре файлов
