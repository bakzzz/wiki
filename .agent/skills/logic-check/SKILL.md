---
name: logic-check
description: >
  Верификация логической связности работы агента. Три фазы: PRE-CHECK (RTM из ТЗ),
  IN-FLIGHT (3 вопроса после каждого шага), POST-CHECK (чеклист + сверка RTM).
  Обнаружение конфликтов в требованиях, отслеживание изменений ТЗ.
  Триггеры: "проверь логику", "logic check", "сверь с ТЗ", "RTM".
---

# Logic Check — Верификация логики работы агента

Скилл для предотвращения потери логической связности при выполнении задач.
Основан на трёх методах: **RTM** (Requirement Traceability Matrix), **CoVe** (Chain-of-Verification), **Checklist Prompting**.

> **КРИТИЧНО!** Этот скилл применяется к ЛЮБОЙ задаче ≥ 3 шагов.
> Для задач < 3 шагов достаточно IN-FLIGHT проверки.

---

## Когда активировать

| Ситуация | Действие |
|----------|----------|
| Задача ≥ 3 файлов/шагов | Полный цикл (PRE + IN-FLIGHT + POST) |
| Задача 1-2 файла | Только IN-FLIGHT (3 вопроса) |
| Пользователь меняет ТЗ | Обновить RTM → Conflict Check → продолжить |
| Пользователь говорит «сверь с ТЗ» | Запустить POST-CHECK немедленно |

---

## Фаза 1: PRE-CHECK (до выполнения)

### Шаг 1.1: Извлечение требований

Из запроса пользователя извлечь ВСЕ требования — явные и неявные.

```
Формат RTM:

| # | Требование | Тип | Файл | Строка | Статус | Версия |
|---|-----------|-----|------|--------|--------|--------|
| 1 | [что сделать] | Явное | — | — | 🔲 TODO | v1 |
| 2 | [что подразумевается] | Неявное | — | — | 🔲 TODO | v1 |
```

**Типы требований:**
- **Явное** — пользователь прямо сказал
- **Неявное** — логически вытекает (например, «добавь поле» → нужна миграция)
- **Ограничение** — что НЕ делать (например, «не трогай файл X»)

### Шаг 1.2: Проверка конфликтов

Для КАЖДОЙ пары требований (i, j) где i ≠ j:

```
Вопрос: "Может ли требование #i противоречить #j?"

Типы конфликтов:
  ❌ ПРЯМОЙ    — взаимоисключающие действия
  ⚠️ КОСВЕННЫЙ — не противоречат напрямую, но создают проблему
  🔄 ЗАМЕНА    — новое заменяет старое
  ✅ НЕТ       — совместимы
```

**При обнаружении конфликта ❌ или ⚠️:**
1. ОСТАНОВИТЬСЯ
2. Показать оба требования пользователю
3. Показать последствия каждого варианта
4. Спросить: «Какое оставить?»
5. Пометить отменённое как `⛔ Отменено` в RTM

> **ПРАВИЛО:** Агент НИКОГДА не разрешает конфликт сам — он ОБНАРУЖИВАЕТ и СПРАШИВАЕТ.

### Шаг 1.3: Генерация чеклиста

Из RTM создать чеклист верификации:

```
Чеклист (из RTM):
[ ] Требование #1: [проверочный вопрос Yes/No]
[ ] Требование #2: [проверочный вопрос Yes/No]
...
[ ] Нет побочных эффектов?
[ ] Все файлы, которые НЕ должны меняться — не изменены?
```

### Шаг 1.4: План выполнения

Определить:
- Порядок выполнения требований
- Зависимости между ними
- Точки проверки (после каких шагов запускать IN-FLIGHT)

---

## Фаза 2: IN-FLIGHT CHECK (во время выполнения)

### После КАЖДОГО изменённого файла — 3 вопроса:

```
┌─────────────────────────────────────────────────┐
│ IN-FLIGHT CHECK                                 │
│                                                 │
│ 1. СВЯЗЬ: Это изменение НАПРЯМУЮ связано с      │
│    КОНКРЕТНЫМ требованием RTM? Каким?            │
│    → Если нет — ОСТАНОВИТЬСЯ, объяснить.         │
│                                                 │
│ 2. БЕЗОПАСНОСТЬ: Это изменение НЕ ЛОМАЕТ        │
│    существующую функциональность?                │
│    → Если не уверен — проверить перед продолжением│
│                                                 │
│ 3. ПОЛНОТА: Нет ли ПРОПУЩЕННЫХ требований,      │
│    которые ЗАВИСЯТ от этого шага?                │
│    → Если да — добавить в план.                  │
└─────────────────────────────────────────────────┘
```

### Обновление RTM

После каждого шага обновить статусы в RTM:

```
| # | Требование | Файл | Строка | Статус |
|---|-----------|------|--------|--------|
| 1 | Поле email | user.py | L42 | ✅ Done |  ← обновлено
| 2 | Валидация  | —    | —      | 🔲 TODO |
```

### При получении НОВОГО требования от пользователя

```
1. ОСТАНОВИТЬСЯ. Не продолжать текущий шаг.
2. Добавить требование в RTM с меткой 🆕 и новой версией.
3. Conflict Check: проверить ВСЕ существующие требования.
   - Конфликт ❌ → спросить пользователя.
   - Конфликт ⚠️ → предупредить, предложить варианты.
   - Замена 🔄 → пометить старое ⛔, добавить новое.
   - Нет конфликта ✅ → продолжить.
4. Impact Check: проверить, затрагивает ли новое уже СДЕЛАННЫЕ пункты.
   - Если да → пометить как 🔁 Требует переделки.
5. Обновить чеклист верификации.
6. Продолжить работу с обновлённой RTM.
```

---

## Фаза 3: POST-CHECK (после выполнения)

### Шаг 3.1: Пройти чеклист

Для каждого пункта чеклиста дать ответ Yes/No с доказательством:

```
[✅] Требование #1: поле email добавлено? → Да, user.py:L42
[✅] Требование #2: валидация email? → Да, validators.py:L18
[❌] Требование #3: тест на email? → Нет, ПРОПУЩЕНО
[✅] Нет побочных эффектов? → Да, другие файлы не затронуты
```

### Шаг 3.2: Сверить RTM

Для каждого требования:
- **✅ Done** — указать КОНКРЕТНЫЙ файл:строку
- **❌ Miss** — объяснить ПОЧЕМУ не выполнено
- **⛔ Отменено** — указать кем и когда
- **🔁 Переделано** — указать причину переделки

### Шаг 3.3: Контрольные вопросы (CoVe)

Сгенерировать 3 вопроса в формате:
```
"Если пользователь [действие], он увидит [ожидаемый результат]?"
```

Ответить на каждый.

### Шаг 3.4: Финальный отчёт

```
📋 LOGIC CHECK REPORT
═════════════════════

RTM: [N/M] требований выполнено ([X]% покрытие)
Чеклист: [N/M] пунктов ✅
Конфликты: [N] обнаружено, [N] разрешено
Версий ТЗ: [N]
Контрольные вопросы: [N/3] ✅

Пропущено:
- [список пропущенных требований, если есть]

Переделано:
- [список переделанных шагов, если были]
```

---

## Шаблон RTM

Полный шаблон: [rtm-template.md](references/rtm-template.md)

Краткий формат:

```markdown
## RTM — [название задачи]

| # | Требование | Тип | Файл | Строка | Статус | Версия |
|---|-----------|-----|------|--------|--------|--------|
| 1 | ... | Явное | — | — | 🔲 TODO | v1 |

### Конфликты
Нет обнаруженных конфликтов.

### История изменений
- v1 (начало): [N] требований из запроса пользователя.
```

---

## Правила обнаружения конфликтов

Полные правила: [conflict-rules.md](references/conflict-rules.md)

### Быстрая таблица конфликтов

| Действие A | Действие B | Тип конфликта |
|-----------|-----------|:-------------:|
| Добавить X | Удалить X | ❌ Прямой |
| X обязательное | X необязательное | ❌ Прямой |
| Не трогать файл | Изменить файл | ❌ Прямой |
| X unique | X optional | ⚠️ Косвенный |
| Добавить CI | Не добавлять файлы в VCS | ⚠️ Косвенный |
| Использовать int ID | Использовать UUID | 🔄 Замена |
| SSH авторизация | HTTPS авторизация | 🔄 Замена |

---

## Интеграция с другими скилами

| Скилл | Как интегрируется |
|-------|-------------------|
| `fullstack-review` | POST-CHECK запускается перед review |
| `review-revisor` | Ревизор проверяет наличие RTM в отчёте |
| `fix-*` | Каждый fixer получает RTM из аудита |
| `git` | Коммит-сообщение ссылается на # требование |

---

## Ограничения

- НЕ разрешать конфликты самостоятельно — только обнаруживать и спрашивать
- НЕ удалять требования из RTM — только помечать ⛔
- НЕ пропускать IN-FLIGHT даже «для быстроты»
- НЕ считать неявные требования подтверждёнными — спросить пользователя
- НЕ игнорировать версионирование — каждое изменение ТЗ = новая версия
