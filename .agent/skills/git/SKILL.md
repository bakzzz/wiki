---
name: git
description: >
  Стандарты и правила работы с Git: branching strategy, коммиты, merge/rebase,
  code review, конфликты, теги, релизы, .gitignore, hooks. Используй при любых
  операциях с Git: коммиты, создание веток, merge, resolve conflicts, release.
---

# Git — Правила и стандарты

Единые правила работы с Git для всех проектов.

---

## Наша архитектура Git

### Per-project репозитории

Каждый проект = отдельный GitHub-репозиторий:

```
~/dev/
├── antigravity/     →  github.com/bakzzz/antigravity  (private)
├── medbot/          →  github.com/bakzzz/medbot        (private)
├── next-project/    →  github.com/bakzzz/next-project  (private)
└── ...
```

| Параметр | Значение |
|----------|----------|
| GitHub user | `bakzzz` |
| Visibility | Private |
| Branch default | `main` |
| Git user | `Денис Афанасьев <bakzzz@gmail.com>` |
| Auth | HTTPS + credential helper (`gh auth`) |
| SSH key | `~/.ssh/id_ed25519` (GitHub: `antigravity-dev`) |

### Создание нового проекта

При создании через `agent-creator` — git и GitHub repo создаются автоматически.

Вручную:
```bash
cd ~/dev/<project>
git init && git branch -m main
git add -A && git commit -m "🚀 Initial commit"
gh repo create <project> --private --source=. --remote=origin --push
```

### Коммит после изменений

```bash
cd ~/dev/<project>
git add -A
git commit -m "type(scope): описание"
git push
```

### Bootstrap на новой машине

```bash
bash ~/.agent/skills/server/scripts/bootstrap-dev.sh
```

Скрипт клонирует все repos из `github.com/bakzzz` в `~/dev/`.

### Что НЕ попадает в Git

- `08_SECRETS.md` — секреты проекта
- `.env`, `.env.local` — переменные окружения
- `node_modules/`, `__pycache__/`, `.venv/` — зависимости
- `*.log` — логи

---

## Branching Strategy

### Выбор стратегии

| Стратегия | Когда использовать | Сложность |
|-----------|-------------------|----------|
| **Gitflow** | Плановые релизы, большие команды, множество версий | Высокая |
| **GitHub Flow** | Continuous deployment, малые команды | Низкая |
| **Trunk-Based** | Быстрая интеграция, зрелые CI/CD, feature flags | Средняя |

> По умолчанию используем **Gitflow**. Для конкретного проекта указать в `docs/deployment.md`.
> **Тренд 2024-2025:** Trunk-Based Development (TBD) — доминирующая стратегия для команд с CI/CD.
> При TBD используйте **feature flags** (LaunchDarkly, Unleash, env vars) для управления фичами в production.

### Основные ветки

| Ветка | Назначение | Защищена | Деплой |
|-------|-----------|----------|--------|
| `main` | Production-ready код | ✅ | Production |
| `develop` | Интеграционная ветка | ✅ | Staging |

### Рабочие ветки

```
main
 └── develop
      ├── feature/<task-id>-<short-description>
      ├── fix/<task-id>-<short-description>
      ├── hotfix/<task-id>-<short-description>
      ├── refactor/<short-description>
      └── docs/<short-description>
```

### Правила именования веток

- Всегда **kebab-case**: `feature/add-user-auth`
- Префикс по типу работы: `feature/`, `fix/`, `hotfix/`, `refactor/`, `docs/`
- При наличии task ID — начинать с него: `feature/123-add-user-auth`
- Максимальная длина: 50 символов после префикса
- Запрещено: пробелы, кириллица, заглавные буквы

### Жизненный цикл ветки

1. Создать от `develop` (или `main` для hotfix)
2. Работать, коммитить
3. Перед merge — rebase на актуальный `develop`
4. Создать PR/MR
5. Code review → approve → merge
6. Удалить ветку после merge

```bash
# Создание ветки
git checkout develop
git pull origin develop
git checkout -b feature/123-add-user-auth

# Rebase перед merge
git fetch origin
git rebase origin/develop

# Удаление после merge
git branch -d feature/123-add-user-auth
git push origin --delete feature/123-add-user-auth
```

## Коммиты

### Conventional Commits

Формат: `type(scope): описание`

```
feat(auth): добавить JWT-аутентификацию
fix(api): исправить 500 ошибку при пустом запросе
docs(readme): обновить инструкцию по деплою
refactor(db): вынести queries в отдельный модуль
test(auth): добавить тесты для регистрации
chore(deps): обновить зависимости
style(lint): исправить форматирование
perf(api): оптимизировать SQL-запрос пользователей
ci(github): добавить workflow для тестов
build(docker): обновить Dockerfile
```

### Типы коммитов

| Тип | Когда | Влияет на версию |
|-----|-------|-----------------|
| `feat` | Новая функциональность | MINOR |
| `fix` | Исправление бага | PATCH |
| `docs` | Только документация | — |
| `refactor` | Рефакторинг без изменения поведения | — |
| `test` | Добавление/изменение тестов | — |
| `chore` | Обслуживание, зависимости | — |
| `style` | Форматирование, пробелы, точки с запятой | — |
| `perf` | Оптимизация производительности | PATCH |
| `ci` | CI/CD конфигурация | — |
| `build` | Система сборки, внешние зависимости | — |
| `revert` | Откат предыдущего коммита | — |

### Правила коммитов

- Описание — на **русском** или **английском** (единообразно в проекте)
- Первая буква описания — **строчная**
- Без точки в конце
- Настоящее время, повелительное наклонение: «добавить», не «добавлено»
- Максимум **72 символа** в первой строке
- Один коммит = одно логическое изменение
- **Запрещено**: `fix`, `update`, `changes`, `WIP` без контекста

### Breaking Changes

```
feat(api)!: изменить формат ответа пользователей

BREAKING CHANGE: поле `name` разделено на `firstName` и `lastName`.
Клиенты API должны обновить парсинг ответов.
```

### Тело коммита (опционально)

```
fix(auth): исправить утечку токенов при logout

Проблема: refresh-токены не инвалидировались при выходе,
позволяя использовать их для получения новых access-токенов.

Решение: добавить blacklist для refresh-токенов с TTL.

Closes #456
```

### Revert коммитов

```bash
# Откат конкретного коммита (создаёт новый коммит)
git revert <commit-hash>

# Откат merge-коммита
git revert -m 1 <merge-commit-hash>
```

```
revert: feat(auth): добавить JWT-аутентификацию

This reverts commit abc123. Причина: утечка памяти в JWT middleware.
```

## Merge vs Rebase

### Когда что использовать

| Ситуация | Стратегия | Почему |
|----------|----------|--------|
| Feature → develop | **Squash merge** | Чистая история, один коммит на фичу |
| Hotfix → main | **Merge commit** | Сохранить trace и cherry-pick в develop |
| Обновить ветку из develop | **Rebase** | Линейная история без merge-коммитов |
| Develop → main (релиз) | **Merge commit** | Чёткая точка релиза |

### Правила

- **Никогда** не rebase публичные ветки (`main`, `develop`)
- **Всегда** rebase feature-ветку перед merge
- При конфликтах на rebase — решать покоммитно
- `--force-push` только в своей ветке, никогда в `main`/`develop`

```bash
# Squash merge feature в develop
git checkout develop
git merge --squash feature/123-add-auth
git commit -m "feat(auth): добавить JWT-аутентификацию (#123)"

# Merge hotfix в main
git checkout main
git merge --no-ff hotfix/critical-fix
```

## Разрешение конфликтов

### Алгоритм

1. **Понять причину**: `git log --merge --oneline`
2. **Посмотреть конфликтные файлы**: `git diff --name-only --diff-filter=U`
3. **Разрешить** каждый файл, сохраняя оба изменения когда возможно
4. **Проверить** — запустить тесты после разрешения
5. **Закоммитить**: `git add . && git commit`

### Правила

- Никогда не принимать слепо «ours» или «theirs»
- При сомнениях — обсудить с автором конфликтующего кода
- После разрешения — **обязательно** прогнать тесты
- Конфликты в lock-файлах (`package-lock.json`, `yarn.lock`) — пересгенерировать

```bash
# Просмотр конфликтных файлов
git diff --name-only --diff-filter=U

# После ручного разрешения
git add <resolved-files>
git rebase --continue  # или git commit если merge
```

## Теги и релизы

### Semantic Versioning

Формат: `vMAJOR.MINOR.PATCH`

| Часть | Когда увеличивать | Пример |
|-------|------------------|--------|
| MAJOR | Breaking changes | v2.0.0 |
| MINOR | Новые фичи (обратно совместимые) | v1.1.0 |
| PATCH | Баг-фиксы | v1.0.1 |

### Создание тега

```bash
# Annotated tag (всегда использовать annotated!)
git tag -a v1.2.0 -m "Release v1.2.0: добавлена авторизация, исправлены баги API"
git push origin v1.2.0

# Список тегов
git tag -l "v1.*"
```

### Правила

- Теги — **только annotated** (`-a`)
- Тег ставится **только на `main`**
- Сообщение тега — краткое описание изменений
- После тега — создать GitHub/GitLab Release с changelog

### Автоматизация релизов

```bash
# Автоматическое версионирование и changelog (Node.js)
npx semantic-release

# Или standard-version для ручного управления
npx standard-version           # auto: определяет bump по коммитам
npx standard-version --release-as minor  # принудительно minor
```

Инструменты парсят Conventional Commits и автоматически:
- Определяют следующую версию (feat → minor, fix → patch, BREAKING → major)
- Генерируют `CHANGELOG.md`
- Создают тег и релиз

## .gitignore

### Обязательные исключения

```gitignore
# Зависимости
node_modules/
vendor/
.venv/
__pycache__/

# Сборка
dist/
build/
*.egg-info/

# Окружение
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Логи
*.log
logs/

# Coverage
coverage/
.nyc_output/
htmlcov/
```

### Правила

- `.gitignore` — в корне проекта
- Секретные данные (`.env`, ключи, сертификаты) — **НИКОГДА** не коммитить
- Если случайно закоммитили секрет:
  ```bash
  git rm --cached .env
  echo ".env" >> .gitignore
  git commit -m "chore: убрать .env из отслеживания"
  # Считать секрет скомпрометированным — перевыпустить!
  ```

## Git Hooks

### Рекомендованные hooks

| Hook | Назначение | Пример |
|------|-----------|--------|
| `pre-commit` | Lint, format | `npx lint-staged` |
| `commit-msg` | Валидация формата коммита | `commitlint` |
| `pre-push` | Тесты перед push | `npm test` |

### Рекомендуемые инструменты

| Инструмент | Назначение | Экосистема |
|------------|-----------|------------|
| **Husky** | Git hook manager | Node.js |
| **commitlint** | Enforce Conventional Commits | Node.js |
| **lint-staged** | Lint только staged files | Node.js |
| **pre-commit** | Hook manager | Python |
| **detect-secrets** | Поиск секретов | Любая |
| **gitleaks** | Поиск секретов | Любая |

### Настройка через Husky (Node.js)

```bash
npx husky init
echo "npx lint-staged" > .husky/pre-commit
echo "npx --no -- commitlint --edit \$1" > .husky/commit-msg
```

### Настройка через pre-commit (Python)

```yaml
# .pre-commit-config.yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0
    hooks:
      - id: ruff
      - id: ruff-format
```

### Проверка на секреты

```yaml
# Добавить в .pre-commit-config.yaml
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.4.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
```

```bash
# Или через gitleaks
brew install gitleaks  # macOS
gitleaks detect --source . --verbose
```

> **Критично:** всегда сканировать на секреты перед push. Утечка = компрометация.

## Code Review

### Чеклист для ревью

- [ ] Код соответствует стилю проекта
- [ ] Тесты написаны и проходят
- [ ] Нет захардкоженных секретов
- [ ] Нет закомментированного кода
- [ ] Нет `console.log` / `print` для дебага
- [ ] Коммиты следуют Conventional Commits
- [ ] Документация обновлена (если нужно)
- [ ] Нет лишних файлов (логи, `.env`, сборка)

### Правила PR/MR

- Заголовок — как Conventional Commit: `feat(auth): добавить OAuth`
- Описание — что изменено, зачем, как тестировать
- Один PR = одна задача/фича
- Максимум **400 строк** изменений (больше — разбить)
- Прикреплять скриншоты для UI-изменений
- Линковать задачу: `Closes #123`

## Полезные команды

```bash
# Посмотреть историю компактно
git log --oneline -20

# Найти кто последний менял строку
git blame <file>

# Интерактивный rebase последних N коммитов
git rebase -i HEAD~3

# Stash с описанием
git stash push -m "WIP: начал рефакторинг auth"
git stash list
git stash pop

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Найти коммит, сломавший тесты
git bisect start
git bisect bad HEAD
git bisect good v1.0.0
# Далее git bisect автоматически ищет

# Очистить удалённые ветки
git fetch --prune
git branch -vv | grep 'gone]' | awk '{print $1}' | xargs git branch -D

# Cherry-pick коммита из другой ветки
git cherry-pick <commit-hash>

# Cherry-pick без автокоммита (для правок)
git cherry-pick --no-commit <commit-hash>

# Посмотреть что изменилось между ветками
git log develop..feature/my-feature --oneline
```

### Cherry-pick: когда применять

| Ситуация | Действие |
|----------|----------|
| Hotfix нужен в develop | Cherry-pick из main в develop |
| Один коммит нужен в другой ветке | Cherry-pick конкретного коммита |
| Перенос нескольких коммитов | Лучше rebase или merge |
| Перенос целой фичи | **Никогда** — используй merge |

## Ограничения скила

- **НЕ** менять историю публичных веток (`main`, `develop`)
- **НЕ** использовать `--force` на общих ветках
- **НЕ** коммитить секреты, даже «временно»
- **НЕ** создавать коммиты с пустым описанием
- **НЕ** мержить без прохождения CI
