---
name: review-revisor
description: >
  Жёсткий ревизор качества ревью. Мета-проверка: аудитирует отчёт fullstack-review на полноту,
  глубину и честность. Выявляет: пропущенные пункты чеклиста, поверхностный анализ, ленивые
  оценки без доказательств, завышенные баллы, отсутствие конкретных файлов/строк в замечаниях.
  Использовать после любого ревью/аудита проекта для верификации качества самого ревью.
  Триггеры: "проверь ревью", "ревизор", "аудит аудита", "проверь качество проверки".
---

# Review Revisor

Жёсткий мета-аудитор. Проверяет НЕ проект, а КАЧЕСТВО проведённого ревью.

## Принципы ревизора

1. **Нулевое доверие** — каждая оценка виновна пока не доказана
2. **Требование доказательств** — нет ссылки на файл/строку = нет проблемы = ленивое ревью
3. **Антизавышение** — балл ≥7 без 3+ конкретных доказательств = подозрение на лень
4. **Полнота покрытия** — каждый пункт чеклиста должен быть адресован
5. **Строковый контроль** — каждая строка кода должна быть проверена, а не выборочно
6. **Документация = правда** — каждая строка документации должна соответствовать реальной логике

## Процесс ревизии

### Шаг 1: Загрузить исходные чеклисты

Загрузить все 6 reference-чеклистов из `~/.agent/skills/fullstack-review/references/`:
- `frontend.md`, `backend.md`, `security.md`, `qa.md`, `devops.md`, `ux.md`

Подсчитать общее количество пунктов в каждом чеклисте.

### Шаг 2: Проверка полноты (Coverage Audit)

Для каждого из 6 критериев сверить:

- [ ] **Каждый пункт** чеклиста адресован в отчёте (не пропущен)
- [ ] Пропущенные пункты — перечислить
- [ ] Процент покрытия: `адресовано / всего × 100%`

Критерии прохождения — см. [references/revisor-criteria.md](references/revisor-criteria.md)

### Шаг 2.5: Проверка Logic Check (RTM Audit)

Если задача выполнялась с `logic-check` — проверить:

1. **RTM существует** — есть ли RTM-таблица в отчёте или рядом с ним?
   - Нет RTM при задаче ≥ 3 шагов → штраф -5 баллов
2. **RTM полная** — все ли требования из ТЗ перечислены?
   - Пропущенные требования → перечислить
3. **RTM с доказательствами** — для ✅ Done есть файл:строка?
   - Нет ссылок → штраф за ленивую RTM
4. **Конфликты разрешены** — если были конфликты, есть ли решение от пользователя?
5. **Версионирование** — если ТЗ менялось, есть ли v2/v3 в RTM?

### Шаг 3: Строковый контроль кода (Line-by-Line Audit)

Ревью должно доказать, что **каждый исходный файл** проекта был открыт и проанализирован.

1. Составить полный список исходных файлов проекта:
```bash
find . -type f \( -name '*.js' -o -name '*.ts' -o -name '*.py' -o -name '*.go' -o -name '*.java' -o -name '*.rb' -o -name '*.rs' -o -name '*.vue' -o -name '*.svelte' -o -name '*.jsx' -o -name '*.tsx' -o -name '*.css' -o -name '*.html' \) -not -path './node_modules/*' -not -path './.git/*' -not -path './vendor/*' -not -path './.venv/*' -not -path './dist/*' -not -path './build/*' | sort
```

2. Сверить со списком файлов, упомянутых в отчёте ревью:
- [ ] **Каждый исходный файл** фигурирует хотя бы в одном замечании или отмечен как проверенный
- [ ] Нет файлов, которые были полностью проигнорированы
- [ ] Для файлов >50 строк — ревью должно упоминать конкретные строки/функции, а не только имя файла
- [ ] Для файлов >200 строк — ревью должно содержать ≥2 замечания или явное подтверждение проверки

3. Перечислить непроверенные файлы:
```
⚠️ Не упомянуты в ревью:
- src/utils/helpers.js (145 строк)
- src/services/payment.ts (230 строк) ← КРИТИЧНО
```

**Штрафы:**
- Пропущен файл <50 строк → -1 балл доверия
- Пропущен файл 50-200 строк → -3 балла доверия
- Пропущен файл >200 строк → -5 баллов доверия
- Пропущен файл с "auth/pay/security" в имени → -10 баллов доверия

### Шаг 4: Проверка документации vs реализация (Docs Logic Audit)

Для **каждого файла документации** проекта (README, CONTRIBUTING, API docs, JSDoc, docstrings, комментарии):

1. Составить список документации:
```bash
find . -type f \( -name 'README*' -o -name 'CONTRIBUTING*' -o -name 'CHANGELOG*' -o -name '*.md' -o -name 'docs/*' \) -not -path './node_modules/*' -not -path './.git/*' | sort
```

2. Для каждого документа проверить что ревью верифицировало:
- [ ] **Каждое утверждение** в документации соответствует фактической реализации
- [ ] Описанные API эндпоинты реально существуют и работают как описано
- [ ] Примеры кода в документации запускаемы и актуальны
- [ ] Описанные конфигурации (env vars, flags) реально используются в коде
- [ ] Инструкции по установке/запуску корректны
- [ ] Описанная архитектура соответствует реальной структуре файлов
- [ ] Нет «мёртвых» ссылок в документации (на удалённые файлы, функции, маршруты)

3. Перечислить несоответствия:
```
🔴 Документация не соответствует коду:
- README.md:15 — описан эндпоинт POST /api/users, но в коде routes.ts он DELETE
- docs/api.md:42 — env var DB_HOST, но в коде используется DATABASE_URL
- README.md:30 — "npm run test" но в package.json скрипт называется "npm run tests"
```

**Штрафы:**
- Документация не проверена на соответствие коду → -5 баллов доверия за файл
- Найдено несоответствие, не замеченное ревью → -3 балла доверия за каждое
- Примеры кода в docs не проверены на работоспособность → -2 балла доверия

### Шаг 5: Проверка глубины (Depth Audit)

Для каждого замечания в отчёте проверить:

- [ ] Указан **конкретный файл** (не "в проекте есть проблемы")
- [ ] Указана **строка или функция** (не "в файле есть проблемы")
- [ ] Есть **объяснение** почему это проблема
- [ ] Есть **рекомендация** как исправить

### Шаг 6: Проверка честности оценок (Score Audit)

Для каждого балла проверить:

- [ ] Оценка ≥8 → должно быть ≥3 конкретных доказательства качества
- [ ] Оценка ≤3 → должно быть ≥3 конкретных примера критических проблем
- [ ] Оценка 5-7 → баланс проблем и достоинств показан
- [ ] Нет "круглых" оценок без обоснования (все 5/10, все 7/10)
- [ ] Разброс оценок реалистичен (все 10/10 = подозрение на лень)

### Шаг 7: Проверка антилени (Laziness Audit)

Выявить признаки ленивого ревью:

- [ ] Копипаста фраз между критериями
- [ ] Общие фразы без конкретики ("код хорошо структурирован", "всё в порядке")
- [ ] Одинаковые оценки по всем критериям (±1 балл)
- [ ] Отсутствие негативных находок (нет идеального проекта)
- [ ] Менее 3 замечаний на критерий
- [ ] Рекомендации абстрактные ("улучшить тесты" вместо "добавить unit-тесты для UserService.authenticate")
- [ ] Файлы упомянуты, но без конкретных строк ("в auth.ts есть проблемы" — какие именно?)
- [ ] Документация отмечена как "ок" без указания что именно проверено

### Шаг 8: Вынести вердикт

Формат вердикта — см. [references/revisor-criteria.md](references/revisor-criteria.md)

## Дополнительно: Верификация fix-ов (при работе в цикле исправления)

Когда ревизор проверяет исправления (не только ревью), дополнительно:

### Git-diff анализ (из Trail of Bits differential-review)

```bash
# Получить diff последних изменений
git diff HEAD~1 --stat           # обзор: какие файлы изменены
git diff HEAD~1 -- <file>        # детальный diff конкретного файла
git log --oneline -5             # последние коммиты
```

Проверить для каждого изменённого файла:
- [ ] Изменение **минимальное** — только то что нужно для fix-а
- [ ] Нет случайных/посторонних изменений (formatting, пробелы)
- [ ] Фиксер **не вышел за границы своего домена** (frontend не трогал backend)
- [ ] Удалённый код действительно не нужен
- [ ] Новый код не дублирует существующий

### Обязательные проверки после fix-ов

```bash
# Запустить и убедиться что всё проходит:
npm run lint 2>&1 || echo "LINT FAILED"
npm run typecheck 2>&1 || npx tsc --noEmit 2>&1 || echo "NO TYPECHECK"
npm test 2>&1 || echo "TESTS FAILED"
npm run build 2>&1 || echo "BUILD FAILED"
```

Если хоть один шаг упал → fix **откатить** и переделать.
