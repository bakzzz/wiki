---
name: incremental-progress
description: >
  Система движения мелкими достижениями (Micro-Achievement Loop). Создаёт план работы,
  декомпозирует на мелкие шаги, обеспечивает обязательные артефакты на каждом шаге и
  сохранение контекста. Предотвращает потерю фокуса и context drift в длинных задачах.
  Триггеры: "создай план", "разбей на шаги", "incremental", "пошаговый план",
  "план работы", "декомпозируй задачу".
---

# Incremental Progress — Движение мелкими достижениями

Скилл для **структурированного движения** по задачам с обязательными артефактами на каждом шаге.
Базируется на модели **Micro-Achievement Loop (MAL)**: PLAN → DO → CHECK → SAVE → REPORT.

> **ПРИНЦИП:** Нет артефакта = нет прогресса. Каждый шаг производит проверяемый результат.

---

## Когда активировать

| Ситуация | Действие |
|----------|----------|
| Любая задача ≥ 3 шагов | Полный MAL-цикл |
| Задача 1-2 шага | Только DO → CHECK → REPORT |
| Длинная задача (≥ 10 шагов) | MAL + Progressive Summary каждые 3-5 шагов |
| Пользователь говорит «план» | Создать план и показать |
| Потеря контекста / длинная сессия | Инициировать Context Recovery |

---

## Фаза 1: PLAN — Декомпозиция задачи

### Шаг 1.1: Извлечение цели

Из запроса пользователя определить:

```
🎯 Цель: [одно предложение — что в итоге должно получиться]
📦 Артефакт-результат: [что конкретно будет создано/изменено]
⏱️ Оценка: [сколько шагов / файлов]
```

### Шаг 1.2: Декомпозиция на уровни

```
Уровень X — Фаза (крупный блок работы)
  └─ Уровень X.Y — Подзадача (конкретное действие)
       └─ Уровень X.Y.Z — Мелкий шаг (одна правка)
```

**Правила декомпозиции:**
1. **Один шаг = одно конкретное действие** (создать файл, изменить функцию, добавить тест)
2. **Каждый шаг имеет проверяемый результат** (тест проходит, файл создан, build зелёный)
3. **Максимальная длительность шага: 10-15 минут** работы агента
4. **Зависимости явные** — шаг B зависит от шага A? Указать.

### Шаг 1.3: Создание Progress Map

```markdown
## Progress Map — [название задачи]

### Фаза 1: [название]
- [ ] 1.1 [действие] → артефакт: [что создаётся] → проверка: [как проверить]
- [ ] 1.2 [действие] → артефакт: [что создаётся] → проверка: [как проверить]

### Фаза 2: [название]
- [ ] 2.1 [действие] → артефакт: [что создаётся] → проверка: [как проверить]
  - [ ] 2.1.1 [мелкий шаг] → проверка: [как проверить]

### Checkpoint: [после какой фазы summary]
```

### Шаг 1.4: Показать пользователю

Показать Progress Map и спросить:
- Всё ли учтено?
- Правильный ли порядок?
- Нужно ли что-то убрать или добавить?

> **ПРАВИЛО:** Не начинать работу без подтверждения плана пользователем.

---

## Фаза 2: DO — Выполнение шага

### Правило одного шага

```
┌─────────────────────────────────────────┐
│            ОДИН ШАГ = ОДИН ЦИКЛ        │
│                                         │
│  1. Объявить: "Начинаю шаг [X.Y]"       │
│  2. Выполнить ОДНО конкретное действие  │
│  3. Не перепрыгивать к следующему шагу  │
│     без завершения текущего             │
└─────────────────────────────────────────┘
```

### Обязательные артефакты шага

Каждый завершённый шаг обязан произвести **минимум один** из:

| Тип артефакта | Примеры |
|---------------|---------|
| **Файл** | Создан/изменён файл (diff) |
| **Результат** | Тест пройден, build зелёный, скриншот |
| **Документ** | Обновлён план, создана документация |
| **Решение** | Зафиксировано архитектурное решение |

---

## Фаза 3: CHECK — Проверка шага

### После КАЖДОГО шага — верификация:

```
✅ Шаг [X.Y] завершён?
├── Артефакт создан? [Да/Нет — какой]
├── Проверка пройдена? [Да/Нет — как]
├── Побочных эффектов нет? [Да/Нет]
└── Готов к следующему шагу? [Да/Нет]
```

**Если ответ «Нет» на любой пункт:**
1. НЕ переходить к следующему шагу
2. Исправить проблему
3. Повторить CHECK

---

## Фаза 4: SAVE — Сохранение контекста

### Обновить Progress Map

```diff
- [ ] 1.1 Создать модель User → артефакт: user.py
+ [x] 1.1 Создать модель User → артефакт: user.py:L1-L45 ✅
```

### Обновить task.md

Пометить текущий шаг как `[x]`, следующий как `[/]`.

### Progressive Summary (каждые 3-5 шагов или при смене фазы)

Создать краткую summary того, что сделано:

```
### Summary после фазы 1:
Создана модель User (user.py), миграция (001_user.py),
валидация email (validators.py). Все тесты проходят.
Готово к фазе 2: API endpoints.
```

> **ПРАВИЛО:** Summary пишется в walkthrough.md или в артефакт задачи.
> Это critical point для восстановления контекста при обрыве сессии.

---

## Фаза 5: REPORT — Статус-футер

Каждое сообщение с действиями заканчивается:

```
---
📊 **Статус [X.Y]**
- Действия: [что конкретно сделано]
- Скилы: [какие применены]
- Прогресс: [N/M шагов выполнено]
- Следующий шаг: [X.Y+1 — что дальше]
```

---

## Context Recovery — Восстановление при потере контекста

Если агент «потерялся» или началась новая сессия:

```
1. Прочитать task.md → что осталось сделать
2. Прочитать Progress Map → текущий прогресс
3. Прочитать последнюю Summary → что было сделано
4. Определить текущий шаг → продолжить с него
5. НЕ начинать с нуля — всегда resume
```

### Автоматические точки восстановления

| Событие | Артефакт для восстановления |
|---------|----------------------------|
| Обрыв сессии | task.md + walkthrough.md |
| Смена фазы | Progressive Summary |
| Завершение задачи | Финальный walkthrough.md |
| Git commit | Commit message с номером шага |

---

## Шаблон Progress Map

Полный шаблон: [progress-map-template.md](references/progress-map-template.md)

Краткий формат:

```markdown
## Progress Map — [задача]

🎯 Цель: [одно предложение]
📦 Результат: [что получится]
⏱️ Шагов: [N]

### Фаза 1: [название]
- [ ] 1.1 [действие] → [артефакт] → [проверка]
- [ ] 1.2 [действие] → [артефакт] → [проверка]
📌 Checkpoint: summary после фазы 1

### Фаза 2: [название]
- [ ] 2.1 [действие] → [артефакт] → [проверка]
📌 Checkpoint: summary после фазы 2
```

---

## Интеграция с другими скилами

| Скилл | Как интегрируется |
|-------|-------------------|
| `logic-check` | RTM создаётся параллельно с Progress Map |
| `fullstack-review` | Progress Map как чеклист для ревью |
| `fix-*` | Каждый fixer следует MAL-циклу |
| `git` | Commit после каждой завершённой подзадачи [X.Y] |

---

## Ограничения

- НЕ пропускать шаги «для быстроты» — каждый шаг обязателен
- НЕ объединять несколько шагов в один — декомпозиция обязательна
- НЕ переходить к следующему шагу без CHECK текущего
- НЕ удалять пункты из Progress Map — только помечать ✅ или ⛔
- НЕ начинать работу без показа плана пользователю (для задач ≥ 3 шагов)
- НЕ забывать Progressive Summary — потеря контекста = потеря работы
